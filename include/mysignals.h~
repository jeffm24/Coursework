#ifndef _SERVER_H_
#define _SERVER_H_

#include "my.h"
#include <signal.h>
/*
 * Author: Jeff Mariconda
 * Class: CS-392
 *
 *
 */

#ifndef _CLIENT_H_
#define _CLIENT_H_

typedef struct s_ack {
  char ack;
} gl_ack;

#endif

typedef struct s_env {
  pid_t clipid;
  char done;
} gl_env;

void send_char(char, pid_t);

void get_char(int);

#endif
